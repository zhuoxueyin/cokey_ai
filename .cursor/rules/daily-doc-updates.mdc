---
description: 每日收尾必须更新 agent.md 与迭代记录
alwaysApply: true
---

# 每日开发收尾：文档与迭代记录（强制）

当一天的开发/调试/优化进入“收尾”阶段时，必须完成以下两件事（不以是否提交/是否发版为前提）：

## 1) 更新 `agent.md`

- **要求**：把当天新增/变更的功能、架构、规则、路由、运维动作、已知遗留同步到 `agent.md`
- **写法**：优先更新文首快照（`§零`）与技术债/排查路径（`§十二`），避免把关键事实散落到其他文档
- **约束**：只写“稳定结论”，细节以代码为准；避免写不可验证的口号

## 2) 新增或更新当日迭代记录

- **目录**：`docs/iterations/`
- **文件名**：`YYYY-MM-DD.md`
- **内容结构**：背景/目标、完成项、修复/优化、已知遗留、验证（参考 `docs/iterations/README.md`）
- **原则**：一页以内、能回溯“为什么/怎么改/影响面/怎么验证”

## 3) 一致性检查（简要）

- `agent.md` 中提到的关键入口/路径/模块名应与代码一致
- 若更新涉及协议字段或接入流程，同时同步 `PROTOCOL_SPEC.md` / `docs/ONBOARDING_SOP.md`（如适用）

