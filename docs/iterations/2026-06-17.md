# 2026-06-17 迭代摘要

> 优化截止快照；对应 `agent.md` **v1.2.0**。

## 背景 / 目标

- 打通 APIYI 对话式生图、协议槽位与 body 入参配置化
- 排查火山视频两段式任务卡住、链路/任务管理看不到 `task_id` 等问题
- 修复创作台素材上传、信息流图片下载等体验问题
- 将当前系统能力整理进 `agent.md`

## 完成项

### 协议与网关

- **InvocationMode** + **ProtocolProfile** + `binding.mode_profiles` 路由（`protocol_resolver.resolve_route`）
- 独立 **ApiyiAdapter**：对话式 `chat/completions` 生图；槽位 `openai.chat.image.text_to_image` / `image_to_image`
- **body_params v2**：`body_param_resolver` + 前端 `bodyParamPresets` + ChannelAdmin 入参模板
- 更新 `docs/ONBOARDING_SOP.md` 接入流程与 body 入参说明

### 火山视频

- `_finalize_volcengine_video_body`：body_params 误配时空 `{}` 兜底
- 创建任务后**立即**持久化 `external_task_id`（`base._persist_external_task_id`）
- `GET /api/tasks/{id}` 对 processing 视频自动向渠道同步；`/recover` 复用同一逻辑

### 管理端

- **TaskAdmin**：列表/详情展示真实 `task_id`；支持 task_id / trace_id 搜索；默认时间窗 24h
- **TraceLogAdmin**：URL 带 trace 时自动筛选；任务 ID 可跳转任务管理
- 管理端任务列表 API 增加 `task_id`、`trace_id` 查询参数

### 创作台体验

- **ComposerArea**：「本地上传」Tab 点确定不再误报「Base64/CDN」错误
- **ChatArea** + `utils/download.ts`：CDN 图片 `fetch→Blob` 下载（预览弹窗与列表）
- **ComposerArea**：视频配置摘要改为读 `currentState`（非写死文案）
- 接入工单 / 模型列表 `page_size` 上限修正为 100

## 修复 / 优化

| 问题 | 处理 |
|------|------|
| trace 长时间 processing、无 channel 响应 | 厘清：平台层同步阻塞轮询；trace 在 generate 结束前不 finalize |
| 任务管理显示 Mongo `id` 而非 `task_xxx` | TaskAdmin 列与详情改为 `task_id` |
| 跨域 CDN `<a download>` 无效 | `downloadRemoteFile` |
| 火山 curl 导入 body_params 全 dynamic 导致空 body | finalize 兜底 + 运营建议清空错误 body_params |

## 已知遗留

- `POST /api/tasks/generate` 仍**同步阻塞**至渠道完成（视频最长约 20min），宜改异步 + 前端轮询
- 无 `mode_profiles` 时 legacy 路由仍可能让 chat 端点影响 image
- ConfigEngine 与适配器内置 `_call_*` 双路径并存

## 验证

- [x] `body_param_resolver` / apiyi / volcengine finalize / invocation_mode 相关单元测试（本地曾跑通）
- [ ] 火山视频端到端长任务（需真实 API Key + 长耗时）
- [ ] 前端刷新后：本地上传确定、信息流下载、任务/trace 互搜

## 文档

- `agent.md` 升级至 v1.2.0（§零 系统快照 + 各章同步）
- 新增本目录 `docs/iterations/` 用于每日迭代归档
