# 2026-06-20 迭代记录

## 背景/目标

重做短剧创作 Agent：保留 Skill/知识库/风格库，以对话式超级智能体替代原 Orchestrator 占位；支持起始对话框 UI 与无限画布右侧可调宽面板。

## 完成项

- **后端**
  - `drama_agent_threads` / `drama_agent_messages` 线程记忆
  - `SuperCreativeAgent`：ModelGateway 文本调用 + 知识/风格/Skill Tool 预检索
  - API：`/api/drama/agent/modes|threads|.../chat`
  - `agent_tools.py`：无 langchain 硬依赖，可选 `to_langchain_tools()`
- **前端**
  - `/drama` → `DramaSuperAgentPage`（参考图起始页 + 对话流）
  - `DramaAgentComposer`：模型/风格/@资源/四快捷模式/多剧集
  - `CanvasEditor` 右下 FAB + `DramaAgentPanel`（280–560px 可拖、localStorage 持久化）
- **测试**：`tests/test_drama_super_agent.py`（mock Gateway）

## 修复/优化

- Composer：去掉「上传剧本」「附件」；风格改为宫格缩略图 Modal
- 四创作类型注入 `agent_mode_prompts.py`（API 不暴露给前端）
- 菜单：资源仓库 / 风格广场一级；Prompt/风格运营归入系统管理
- 全量风格补全 `model_protocol`（图/视频/角色/场景提示词 + trait_tags + preview）

## 已知遗留

- 风格封面图批量上传（当前用渐变 preview）
- 用户风格编辑 UI 细化（API 已有 PUT）

## 验证

```bash
# 后端 import
python -c "from app.services.drama_super_agent_service import get_super_creative_agent; print('ok')"

# 前端：访问 /drama 发送首条消息；打开 /canvas/:id 点击右下机器人 FAB
curl http://127.0.0.1:8001/api/drama/agent/modes
```
